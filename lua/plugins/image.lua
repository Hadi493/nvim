require("image").setup({
  backend = "ueberzug",
  max_width = 100,
  max_height = 50,
  max_width_window_percentage = 50,
  max_height_window_percentage = 50,
  window_overlap_clear_enabled = true,
})
